name: Setup Railway
description: Install the Railway CLI used to trigger a redeploy.

runs:
  using: 'composite'
  steps:
    - name: Install Railway CLI
      shell: bash
      run: npm install -g @railway/cli
